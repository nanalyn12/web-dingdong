# Env (secrets — never commit)
.env
.env.*
!.env.example

# Build info
tsconfig.tsbuildinfo

# Generated media (video renders, thumbnails, fonts) — lives on the volume
data/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Local-only workspace files
.claude/settings.local.json
.workspace/
# 하네스 산출물 (기준·Red 리포트·게이트 판정) — 로컬 감사 기록이라 공유하지 않는다
_workspace/
_workspace_*/

# 논문 원고 (로컬 문서, 저장소에 올리지 않는다)
write.md
new-write.md
소개.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 논문용 이미지 (로컬 산출물)
paper_images/
